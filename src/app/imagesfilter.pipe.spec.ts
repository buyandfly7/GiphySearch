import { ImagesfilterPipe } from './imagesfilter.pipe';

describe('ImagesfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagesfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
