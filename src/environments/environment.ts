// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBy0K0ZKa91Q9QbsEjHOiNZ2r2qniboyLU",
    authDomain: "giphysearch-edaee.firebaseapp.com",
    databaseURL: "https://giphysearch-edaee.firebaseio.com",
    projectId: "giphysearch-edaee",
    storageBucket: "giphysearch-edaee.appspot.com",
    messagingSenderId: "160043698875"
  }
};
